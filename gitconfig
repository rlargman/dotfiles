[user]
	name = Raissa Largman
	email = raissa@linear.app
	username = raissa
[color]
	ui = auto
[core]
	editor = /usr/bin/vim
  excludesfile = ~/.gitignore
	pager = diff-so-fancy | less --tabs=4 -RFX
[push]
	default = current
[rerere]
	enabled = true
[alias]
  prune-merged = ! "git branch --merged master | grep -v -E '^(\\* |  master$)' | xargs -n 1 git branch -d"
